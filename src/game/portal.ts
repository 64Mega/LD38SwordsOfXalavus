// Portals
// Represents a transition from one map to another with a specific entry and return point

import * as maps from "./map";

export function update(delta: number) {

}

export function render() {
    
}